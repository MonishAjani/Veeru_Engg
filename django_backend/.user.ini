; Set Python as the handler
engine=python

; PHP settings (to disable PHP processing)
display_errors=Off
log_errors=On

; Increase memory limit if needed
memory_limit=256M

; Increase max execution time for long-running processes
max_execution_time=300